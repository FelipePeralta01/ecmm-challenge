[project]
name = "backend"
version = "0.1.0"
description = "Backend REST API para catálogo de productos - Django & DRF"
requires-python = ">=3.12"
dependencies = [
    "django>=6.1.1",
    "django-cors-headers>=4.9.0",
    "django-filter>=26.1",
    "djangorestframework>=3.18.1",
]
